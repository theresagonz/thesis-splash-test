'use strict';

class Main extends React.Component {
  render() {
    return (<div>Get a website in 15 minutes or less</div>)
    
  }
}

const domContainer = document.querySelector('#root');
ReactDOM.render(<Main/>, domContainer);
